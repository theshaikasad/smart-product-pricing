# Inference + web app
torch>=2.2,<3
open-clip-torch>=2.24
scikit-learn>=1.4,<1.7
numpy>=1.26,<2
pillow>=10
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29
huggingface_hub>=0.23

# Training scripts / notebook (not needed to serve the app)
pandas>=2.0
tqdm>=4.66
joblib>=1.3
